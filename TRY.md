# 给试用的人

本文对应 2026-09 的 **Python 工作台分发包**。Civil Buddy 用于土木企业内部讨论与文书起草，产物不是签认件。

历史 `v0.4.0-workbench` 的 `civil-workbench.exe` 使用旧 Rust 服务，不包含本次 Python 工作台的全部功能。当前源码和新分发包按本文启动；本地打包不代表已经发布到 GitHub Releases。

## 1. 安装与启动

准备 Python 3.11 或更新版本，将完整工作台 zip 解压到自己有写入权限的目录。不要只复制启动器，也不要解压到 `D:\layout`。

双击包内 `start-workbench.bat`。第一次启动会在**包内 `.venv`** 安装 `requirements.txt` 的依赖，需要网络；不会向系统 Python 安装项目依赖。后续启动复用环境，浏览器在服务就绪后打开。

可在 PowerShell 指定端口或修复包内环境：

```powershell
.\start-workbench.bat --port 8765
.\start-workbench.bat --setup
```

已有准备好的 Python 环境时，可设置 `CIVIL_PYTHON` 为该环境的 `python.exe` 完整路径。启动器仅复用其依赖，程序和岗位资料仍读取本包；依赖不足会给出提示。

从 Git 仓库运行：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m packing_assistant.civil app
```

保持服务窗口运行。关闭服务后，页面无法继续处理请求；下次从同一目录启动可恢复任务记录。

## 2. 不填 Key 也能试用

首页选择“项目日报”，输入：

```text
写一份项目日报；辖区：CN；项目名称：东岸改造工程；
日期：2026-09-12；部位：东侧作业面；天气：多云；
出勤：木工12人；安全质量记事：围挡已完成复查。
```

提交后预览 Markdown，并下载 Word、Excel；核对天气、出勤和记事是否出现在对应栏位。Word 中的标题、列表和表格可继续编辑。缺失资料保留 `[A001]` / `UNSPECIFIED` / `TBD`，不会补造事实。某种格式导出失败会明确提示，已生成的其他文件仍可下载。

纯提问不生成业务文件。高风险岗位须在确认卡**完整键入** `我明白，将由持证人员签认` 后再提交；切换任务时确认会清空。

66 岗都有专用起草栏位。设计岗可按“对象名称：……；参数名称：……”逐对象输入，或上传以字段名为首行的表格。数值只抄已给资料；结构计算、设备选型和审批结果由相应工具或责任专业完成，草稿不会代填。

## 3. 附件和任务恢复

回形针支持文本、CSV、JSON、DOCX、XLSX 和含文本的 PDF。附件归属当前任务，只有选中的资料参与本次处理。扫描图片 PDF 会提示无法提取，不能当作已读取。

CSV、Excel 工作表和 Word 表格会保留行列，首行应为“会议名称、场地、议程”等字段名。每行表示一个对象或记录；缺值留空，不借用上一行参数。复杂合并单元格、图片中的表格和无列名数据请先整理成普通表格。未提供辖区时显示 `UNSPECIFIED`；同份资料含多个地区时分别核对对象归属和依据。

每次成功生成的文书会保留可下载快照；刷新页面恢复最近任务的对话、岗位和选中附件。点击“新任务”开始独立会话。

“停止”会请求后台取消，并等待本次运行收尾。模型连接会关闭；已经开始的本地工具完成当前操作后停止后续步骤。页面保留部分回答和实际生成的文件，已完成的写入不会撤销。切换任务、关闭页面或连接断开也会请求取消原任务；收尾完成前不能重复执行同一任务。

迁移或升级前，在“设置 → 备份当前任务”下载 ZIP；在新目录启动工作台后，通过“设置 → 导入任务备份”选中该 ZIP。每次导入都会新建任务，保留完整对话、上传资料和已生成的文书，原任务不受影响。导入任务放在“未归类”，可自行归入项目；高风险操作须重新确认。

备份限单个任务、128 MB，不含模型 Key、全局知识库、自建岗位或外部作业目录。任务运行时须等待结束或停止后再备份。损坏或不受支持的备份会被拒绝，不会覆盖已有任务。项目索引若损坏，工作台会保留原文件并停止修改，避免用空列表覆盖已有项目。

## 4. 可选模型设置

在“设置 → 模型设置”填写兼容接口地址、模型名称和自己的 API Key，保存后可进行模型问答。具体模型与地址以供应商提供的信息为准。

界面保存的 Key 仅在服务进程内存生效，关闭后需重新填写。界面不回显明文，也不把 Key 写进文书。点击“回退到环境配置”恢复启动时环境；可选环境文件为 `demo/.env`：

```env
CIVIL_API_KEY=填写自己的Key
CIVIL_API_BASE=填写兼容接口地址
CIVIL_MODEL=填写模型名称
```

环境文件中的 Key 由你保存在本机，不要提交仓库或发到 Issue。模型配置失败、断流和工具失败会显示错误，不能视为任务成功。

## 5. 可选作业根与装箱服务

`CIVIL_JOB_ROOT` 可指向已有、允许读取和写入业务文件的作业目录，例如 `C:\Users\你\Documents\某工地`。不设置时文书保存在工作台自己的任务目录。点名现有工作簿时只更新约定的 `CB草稿-*` 工作表，保留其他表和公式。

工作台启动器只启动工作台服务。装柜台另由 `gateway.app:app` 提供；界面会按服务能力提示连接状态。需要装箱链路时，在包含 gateway 和装箱资源的完整仓库内另运行：

```powershell
.\.venv\Scripts\python.exe -m uvicorn gateway.app:app --host 127.0.0.1 --port 8000
```

没有接通求解器时，柜数、坐标等保持 `UNSPECIFIED`；`can_fit=false` 是失败。文书始终保留 `submit_blocked=true`，不代投标、开工审批、盖章或签认。

源码 MIT，见 [LICENSE](LICENSE)。详细入口见 [GETTING-STARTED.md](docs/civil-buddy/GETTING-STARTED.md)，实际岗位深度见 [depth-ladder.md](docs/depth-ladder.md)。
