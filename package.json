{
  "name": "civil-buddy",
  "private": true,
  "description": "Civil Buddy Agent Middleware — permission, sandbox, HITL, audit, cost. Runtime, not a prompt.",
  "scripts": {
    "check": "node scripts/npm-check.cjs",
    "check:full": "node scripts/npm-check.cjs --full",
    "demo": "python scripts/demo_agent_middleware.py"
  }
}
