# 工作台优化 · 2026-09-18 —— 移植到 main 的状态（2026-09-21）

原三轮补丁基于 `e923c1f`（2026-08-25）；到 2026-09-21 的 main 上，维护者已用自己的方案重做了其中大半（`demo/chat_service.py`、`turn_control.py`、`sessions`、`uploads`、2026-09-17～20 的 workbench 系列提交）。因此不是 rebase，而是逐项对比后只把 main 上缺的部分按 main 现在的架构移植进来。下面是对照表；原始记录见后半部分。

## 合进来的

| 项 | 落点 |
|---|---|
| 装箱协作式取消（登记表、`scope()/check()`，agent 边界 + `bin3d` 放置/条带循环 + LLM 轮次 + 3D/侧视每柜 + xlsx 导出每行） | `packing_assistant/runtime/cancel.py`、`teams/big_team.py`、`tools/bin3d.py`、`tools/visualize.py`、`export_pack.py`、`agent_loop.py` |
| 网关 `POST /api/runs/{id}/cancel` 接受 run_id 或 session_id；`/api/confirm cancel` 也登记 | `gateway/app.py` |
| `POST /api/export/shipment` 返回 `download_url`；`GET /api/export/file?name=`（限 exports 目录，`../` 404） | `gateway/app.py` |
| 装箱台「导出 xlsx」+ 下载链接；WS 指数退避重连；取消/强制解锁真的取消后台运行 | `frontend/workbench.html` |
| `CIVIL_TOKEN`（Bearer / `?token=` / `cb_token` cookie；`/`、`/static`、`/api/health` 放行；`capabilities.auth`；前端首个 401 弹一次口令写 cookie） | `demo/app.py`、`demo/static/app.js` |
| `CIVIL_HOST` / `CIVIL_PORT` 从 `.env` 读；非回环无口令启动时警告 | `demo/serve.py`、`demo/run.ps1` |
| 模型超时分段：`CIVIL_LLM_READ_TIMEOUT`（180）、`CIVIL_LLM_CONNECT_TIMEOUT`（15） | `demo/llm.py::default_timeout` |
| 手机：`accept` 加 MIME、`interactive-widget=resizes-content`、`visualViewport` 贴合键盘 | `demo/static/index.html`、`app.js` |
| 测试：`scripts/test_cancel_inside_agent.py`、`demo/tests/test_lan_access.py` | |

## 不合了（main 已有等价实现）

- Python 真流式 / 心跳 / `file` 事件 / `error` 带 `partial_text`：main 的 `chat_service.stream_turn` 已有 0.5 s `heartbeat`、断线不取消（`CIVIL_DETACHED_TURN_SECONDS`）、`GET /api/sessions/{sid}` 找回结果；原补丁改的 `demo/agent.py::run_expert` 在 main 上已无调用方。
- `thread_id` 转写 / `/api/threads/{id}/messages|files|cancel` / `DELETE`：main 用 `projects` + `sessions`（`/api/sessions/{sid}`、`/cancel`、`/export`、`/api/session-import`）。
- `CIVIL_DETACH` / 409：main 有 `SessionLease` + `SessionBusy`（409）+ 前端 `cbWatchSession` 轮询。
- `demo/attach.py` / `/api/upload` / `read_attachment`：main 用 `demo/uploads.py` + `/api/upload` + `/api/attachments`。
- RFC 5987 下载名、`crypto.randomUUID` 守卫、`/api/health.capabilities`、抽屉 CSS、Vue 本地化：main 都已有。
- `_CHAT_POOL`：main 的每轮已在独立 producer 线程里跑，同步迭代器只做转发，没必要再加池。
- `scripts/e2e/`（jsdom 流程）：写在原补丁的 threads 接口之上，对 main 的 sessions 接口不适用，没有搬。

## 第二批（2026-09-21，前端为主）：合进 main 之后再看一遍剩下的

| 项 | 做了什么 | 落点 |
|---|---|---|
| Rust `/api/health` 无 `capabilities` → 共用页面在 Rust 上点「上传/备份/停止」得到 404 | Rust health 老实声明能力表；页面在没有 cancel 能力的后端上不再摆「停止」 | `workbench/src/api.rs`、`tests/workbench.rs`、`app.js` |
| 上传没有进度、不能取消、类型/大小/数量全靠服务端拒 | XHR + `upload.onprogress`，chip 上有进度条/百分比/×，失败留在原位可「重试」；并发 2；类型、20 MB、12 个在发前先查，文案同服务端；粘贴的文件也走这条路；切会话中止另一个会话的上传 | `app.js`、`styles.css` |
| 断流回来只有空气泡，直到整轮跑完 | `stream_turn` 把已产出正文/最近状态存内存，`GET /api/sessions/{sid}/live`；`cbWatchSession` 每拍拉一次逐段补画，带「后台进行中 · …」状态行 | `chat_service.py`、`app.py`、`app.js` |
| 切走的任务跑完了没人说 | 列表里还有运行中的会话时每 5 s 拉 `/api/sessions`，变完成的那个弹可点的提示（「查看」直接切过去） | `app.js` |
| 输入框草稿不按会话保存 | `localStorage` 按会话存草稿，切回来填回去，发送即清 | `app.js` |
| 下载链接里是服务器绝对路径 | `/api/file?session=&run=&file=&name=`，`path=` 仍留给 Rust 与旧卡片；`capabilities.file_ref` 告诉页面用哪种 | `app.py`、`app.js` |
| 静态资源版本号手写，手机拿旧缓存 | `GET /` 按文件 mtime 给 `/static/*.js|css` 盖版本；`/` 与 `/static` 都 `Cache-Control: no-cache` | `app.py` |

验证：`demo/tests` 62 通过；`npm run check` 75/75（含 `scripts/test_chat_stream.cjs` 84 例）；Rust 只改了 health 一处，靠 CI 的 rust job 编译。

## 第三批（2026-09-21）：结构项 1 —— 事件日志与续流

- `/api/chat` 每一帧带 `id: <seq>`；`stream_turn` 把本轮事件留在内存并逐行落盘 `out/<sid>/events/<turn_id>.jsonl`（`latest` 指向当前轮）。心跳不编号不落盘。
- `GET /api/sessions/{sid}/events?after=N`（或 `Last-Event-ID`）：回放没看到的帧，轮次还在跑就跟到 `done`；内存表空了（重启后）读文件。`/api/sessions/{sid}` 的 `turn_state` 带 `turn_id`/`seq`。
- 前端一套处理器喂三条路（首次流 / 断线续流 / 切回来全量回放），重复 id 跳过；续流指数退避 1–15 s，直到 done、点停止、或服务端说这轮已结束；切回运行中的会话 = 从 0 回放 + 跟到底，停止按钮照常。`cbWatchSession` + `/live` 只剩给没有 `event_log` 能力的后端（Rust）。
- 结构项 2（运行态落盘、重启判 stale）、3（目录统一）、4（thread 并入 session）、5（后端/前端收敛）、6（app.js 拆模块）未动。

还没做的：`error` 事件带 deliverables（流内报错时已落盘文件不显示）、心跳改注释帧、`pageshow`/`online` 补接、备份导入/导出改流式、聊天气泡的 Markdown 渲染、`app.js` 拆模块、jsdom 流程测试、PWA。

## 待办：Rust 工作台

原第二、三轮的 Rust 改动（`llm.rs::stream_chat` 真流式、`Live` 实时事件、`threads.rs`、`/api/config`、`/api/skills`、`/api/mcp/*`、`CIVIL_HOST`、token 中间件、RFC 5987 `file_get`）**没有合**：移植环境没有能编译该依赖树的 Rust 工具链（需要 edition 2024），不敢盲改。共用前端现在调用的 `/api/config`、`/api/skills`、`/api/upload`、`/api/sessions/{sid}/cancel`、`/api/context*`、`/api/deliverables.zip` 在 Rust 侧仍是 404，由前端按 capabilities 静默跳过。建议在有 `cargo` 的机器上参照 `workbench/src/api.rs` 的 sessions 设计单独做一轮。

---

# 工作台优化 · 2026-09-18（断流 / 任务切换 / 上传下载 / 手机）

范围：Python 工作台 `demo/`、共用前端 `demo/static/`、`packing_assistant/runtime/threads.py`、装箱网关取消与导出、Rust 工作台最小改动。
起因见上一轮验收：两套后端各缺一半路由；"流式"是整轮跑完再切片；中途超时把已落盘文件从 UI 上丢掉；手机进不了门（`127.0.0.1` 硬绑 + `crypto.randomUUID` 非安全上下文）。

## 改了什么

### 断流

| 项 | 之前 | 现在 | 位置 |
|---|---|---|---|
| 流式 | 每步阻塞 `chat()`，最后按 40 字假切片 | `stream_chat()` 逐 token 推送，同时拼装 tool-call delta | `demo/llm.py`、`demo/agent.py::run_expert` |
| 心跳 | 无；模型沉默 >60 s 即被代理/手机网络切断 | `: ping` 每 `CIVIL_SSE_PING_SEC` s（默认 15） | `demo/app.py::chat` |
| 文件 | 只在 `done` 里出现；`done` 不到 = 文件丢 | 落盘即发 `file` 事件；`error` 带 `partial_text/deliverables/citations` | `demo/agent.py` |
| 前端错误 | 用错误文本覆盖已显示正文；history 留下悬空 user | 正文保留 + 状态行；`.interrupted` 标记；history 补 assistant 占位 | `demo/static/app.js` |
| 停止 | 无 | `AbortController` + 停止按钮 + `/stop`；服务端 `should_stop` 在下一 chunk 关闭上游 | `app.js`、`demo/llm.py` |
| 断连 | 同步生成器悬挂，上游继续算 | 响应结束/客户端断开 → `stop.set()` → `LLMStopped` | `demo/app.py` |
| 空 draft.md | 参数 JSON 截断 → 静默写空文件 | 拒绝写空正文，回告模型重发 | `demo/agent.py::execute_tool` |
| 超时 | 单一 120 s | connect/read/write 分开，`CIVIL_LLM_READ_TIMEOUT` 默认 180 | `demo/llm.py` |
| 并发 | 每个对话占默认线程池 | 独立 `_CHAT_POOL`（`CIVIL_MAX_CHATS`），health 等不被拖死 | `demo/app.py` |

### 任务切换

- `/api/chat` 绑定 `thread_id`（没有就新建，`context` 事件回传）；转写按行落 `demo/out/_threads/<id>.messages.jsonl`。
- 新接口：`GET /api/threads/{id}/messages`、`GET /api/threads/{id}/files`、`POST /api/threads/{id}/cancel`、`DELETE /api/threads/{id}`。
- 前端：切线程加载转写+文件+附件并重建 `history`；「新建」清屏；「并行」任务 3 s 轮询，完成后提示；`localStorage` 记住当前线程，刷新恢复；服务端换了 thread_id 时前端跟随。
- `threads.py`：原子写（tmp+rename）；重启后残留 `running` 判 `stale`；`request_cancel/cancel_requested` 协作式取消。
- 历史里连续 user 消息服务端合并。

### 上传 / 下载

- 新 `demo/attach.py`：与 Rust `attach.rs` 同目录布局、同限额（20 MB、12 个、200k 字），docx/xlsx/pdf 抽字（pdf 走 `pypdf`，扫描件明确报错）。
- `POST /api/upload`、`GET /api/attachments`、`POST /api/local`；`ChatIn.attachments` 注入提示词；`read_attachment` 工具。
- 前端 XHR 上传带进度，支持粘贴；下载链接带 `download` 属性，.md/.txt 有「预览」。
- Python `/api/file`：`filename*=utf-8''`、`inline=1`、相对路径；Rust `file_get` 同样改为 RFC 5987 + inline + 相对路径。

### 手机 / 接口对齐

- `uid()` 特性检测（`crypto.randomUUID` 只在安全上下文存在）。
- `/api/health.capabilities`（Python 与 Rust 都返回）；前端按能力隐藏按钮，不再把 404 打进对话。
- CSS：≤1100 px 右栏抽屉，≤720 px 左栏抽屉，`100dvh`、安全区、textarea 16 px 防 iOS 缩放，本机路径行隐藏，工作室单列。
- `CIVIL_HOST`：Rust `main.rs` 读环境变量；Python `demo/serve.py` 读 `.env`。非回环地址启动时打印警告（无鉴权）。
- `packing_assistant/__init__.py` 惰性导入，`demo/requirements.txt` 补齐 —— 按 demo/README 装完能直接起。

### 装箱侧

- `packing_assistant/runtime/cancel.py` 登记表；`teams/big_team.run_one` 在每个 agent 边界检查 run_id/session_id，命中则后续 agent 标 `cancelled` 跳过；新一轮运行先清登记。`POST /api/runs/{id}/cancel` 接受 run_id 或 session_id；`/api/confirm cancel` 也登记。
- `POST /api/export/shipment` 返回 `download_url`；新增 `GET /api/export/file?name=`（限 `<PACKING_OUTPUT_DIR>/exports`）。
- 前端：Vue 本地化 `frontend/vendor/vue-2.7.16.min.js`（CDN 兜底）；WS 指数退避重连；「导出 xlsx」按钮；取消/强制解锁真正取消后台运行。

## 验证

- `demo/tests`：32 通过（新增 `test_stream_resilience.py` 13 例：心跳、文件先于错误、真流式、转写、取消、下载头、上传、路由对齐、安全上下文守卫）。
- `npm run check`：PASS。
- 真 HTTP 端到端（假上游逐 token）：模型沉默 2.5 s 收到 2 个心跳；42 个 token 分布在 10 s 内到达；`file` 事件先于 `done`；中文文件名 `filename*=utf-8''` 正确；客户端 4 s 断开后上游 `aborted=1`、线程 `cancelled`、取消标记清空。
- 装箱网关：`/api/demo` 启动 0.6 s 后按 session 取消，返回 `phase=cancelled`、有 agent 标 `cancelled`；同 session 再跑不受影响；导出下载 xlsx 8 KB，`../` 穿越返回 404。
- 预先存在、与本轮无关的失败：`scripts/test_p0_p1_p2_full.py`（`team_mode` 断言）；`test_kb_prompt` / `test_demo_bid_handoff` 在 Linux `/tmp` 下的 sandbox 拒写已改为设置 `CIVIL_SANDBOX_ROOTS`。

## 第二轮（同日）：把"还没做"做完

- **Rust 工作台真流式**：`llm.rs` 新增 `stream_chat()`（逐 token 回调 + 拼装 tool-call delta，修了跨网络块 SSE 行丢失、按字节切段把中文切成 `�`），`agent.rs` 新增 `Live { sink, stop }` 实时推事件、`file` 事件在 harness 落盘后立刻发；`api.rs` chat 改为 unbounded channel 直出、客户端断开 `StopOnDrop` 即停、心跳间隔读 `CIVIL_SSE_PING_SEC`。
- **Rust `/api/threads` 全套 + `/api/config`**：新 `threads.rs` 与 Python 用同一份磁盘格式（`demo/out/_threads/*.json` + `.messages.jsonl`），两套后端互相看得到对方的对话；capabilities 全部置真。
- **鉴权**：两端都支持 `CIVIL_TOKEN`（Bearer / `?token=` / `cb_token` cookie；`/`、`/static`、`/api/health` 放行），前端首个 401 弹一次口令写 cookie，下载链接和 XHR 上传自动带上。
- **装箱 agent 内部取消**：`runtime/cancel.py` 加 `scope()`/`check()`（登记表为空时零开销），`big_team.run_one` 进 scope 并把 `RunCancelled` 记为 `cancelled` 步；`bin3d` 三个放置循环、LLM 轮次插检查点。600 件装箱在取消后 0.17 s 中断。
- **前端流程 e2e**（`scripts/e2e/`）：jsdom + Node fetch 在 390 px、无 `crypto.randomUUID` 的窗口里跑未改动的 `app.js` 对真后端：抽屉、新建/切换、流式、停止保留正文、文件事件与 RFC 5987 下载头、上传——Python 与 Rust 各 34/34；`stream_checks.py` 做 HTTP 级心跳/实时 token/断连停止验证；CI 加了 Python e2e 步骤和 `rust-workbench` job（`cargo test` + 同一套流程）。
- 修了一处第一轮遗留：`big_team.run_one` 里 `except RunCancelled` 插错位置导致正常路径不合并状态（`test_mid50_uniform` 回归发现）。

验证：`demo/tests` 33 通过；Rust 20 单元 + 4 MCP + 106 集成通过；`npm run check` PASS；`test_mid50_uniform / test_runtime_p0 / test_agent_loop / test_cancel_inside_agent` PASS；两套后端 jsdom 流程 34/34；HTTP 级检查两端 ALL PASS。

## 第三轮（同日）：能做的都做了

- **锁屏/断网不再等于取消**：两端新增 `CIVIL_DETACH`（默认 `continue`）。客户端掉线后服务端继续跑完，回复写进转写；只有显式「停止」（POST cancel）才中断上游。前端区分"用户点停止"与"连接被掐"，后者显示"服务端继续生成中"，按 1.5 s 轮询，完成后自动重绘；`visibilitychange` / `online` / `pageshow` 回来时对比转写条数自动同步。detached 运行未结束时再发消息返回 409，前端把话放回输入框等它完成。HTTP 实测两端：掉线后上游 `aborted=0`、线程 `done`、转写多 2 行；POST cancel 后 `aborted=1`、`cancelled`。
- **Rust `/api/skills` + `/api/mcp/*`**：skills 读 `.agents/skills/*/SKILL.md` frontmatter（与 Python `catalog()` 同形）；MCP 八个路由是 `mcp::handle_rpc` 的薄包装，过滤规则与 `civil-mcp` 一致（construction 看不到招标工具）。capabilities `skills/mcp` 置真。
- **更多取消检查点**：3D/侧视图每个柜面板、导出 xlsx 每柜/每件、条带预装循环。`test_cancel_inside_agent` 新增两例：已取消的 session 导出和渲染都在第一个检查点停下。
- **手机细节**：`accept` 加 MIME（Android 文件选择器识别 docx/xlsx）、`viewport-fit=cover` + `interactive-widget=resizes-content`、`visualViewport` 变化时把布局压到键盘之上并滚到底。
- jsdom 流程增加"锁屏"场景（OS 掐掉 fetch → 服务端跑完 → 回到页面自动同步）：两端 **38/38**。

验证：`demo/tests` 34；Rust 133（含 skills/MCP/409 新用例）；`test_mid50_uniform / test_runtime_p0 / test_agent_loop / test_stack_prefer / test_cancel_inside_agent` PASS；两端 jsdom 38/38 + HTTP 级 ALL PASS。

## 只剩真机

- iOS/Android 真机看一遍视觉：抽屉、键盘遮挡、下载后的分享面板文件名。逻辑层已由 jsdom 覆盖，渲染层没有。
- 取消检查点覆盖了装箱放置、LLM 轮次、渲染、导出；外部 Java 引擎（skjolber）那一步若启用仍要跑完当前调用。

## 手测清单

桌面：① 召唤施工方案 + 勾确认 →「写一份…」→ 生成中拔网线 → 正文保留、文件栏有链接；② 生成中点「停止」→ 状态行「已停止」，可继续发；③「并行」→ 1 分钟内左栏状态变「完」并有提示；④ 下载 `.md` 文件名为中文原名，「预览」新标签直接显示；⑤ 上传 docx 后提问，回答引用其内容。

手机（`CIVIL_HOST=0.0.0.0`）：⑥ 首页 Key 徽章亮；⑦ 顶栏「岗位」抽屉可选岗、「对话」可切线程；⑧ 成稿期间锁屏 40 s 回来，正文在，「文件」抽屉有文件；⑨ 装箱台上传 xlsx → 确认 → 「导出 xlsx」→ 下载。
